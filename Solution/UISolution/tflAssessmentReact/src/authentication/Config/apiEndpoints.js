
const API_BASE_URL_AUTH = import.meta.env.VITE_API_BASE_URL5;


const endpoints = {

    // Authentication endpoints
    Authentication: {
      changePassword: `${API_BASE_URL_AUTH}/auth/updatepassword`,
    }
}